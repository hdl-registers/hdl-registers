# This will allocate a register constant with the name "supports_pre_filtering" of type boolean.
[constant.supports_pre_filtering]

# The "value" property MUST be present for a boolean constant.
# The value specified must be a boolean.
value = true

# The "description" property is optional for a constant. Will default to "" if not specified.
# The value specified must be a string.
description = "Will indicate **True** if the module supports data pre-filtering."

[constant.is_release_version]

value = false
